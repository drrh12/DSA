# Linear Arrays

# Multi-dimensional Arrays

# Dynamic Arrays

# Jagged Arrays

# Arrays operations

# size()
# capacity()
# is_empty()
# at(index)
# push(item)
# insert(index, item)
# prepend(item)
# pop()
# delete(index)
# remove(item)
# find(item)
# resize(new_capacity)

# Time 
# O(1) - add/remove at the end
# O(n) - insert/remove elsewhere

# Space
